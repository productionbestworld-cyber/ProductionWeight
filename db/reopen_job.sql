-- ════════════════════════════════════════════════════════════════
--  ย้อน "ปิดงาน" (เปิดงานกลับมาทำต่อ) — ไม่แตะม้วน/ข้อมูลอื่น
--  วิธีใช้: แก้ค่า :lot ให้เป็น Lot ที่ต้องการ แล้ว Run เฉพาะบล็อกที่ต้องการ
-- ════════════════════════════════════════════════════════════════

-- ───────────────────────────────────────────────────────────────
--  A) ย้อนปิด "งานกรอ" (rework_jobs) — กลับเป็น active
--     ใช้กับงานที่กดปิดในหน้า "งานกรอ"
-- ───────────────────────────────────────────────────────────────
UPDATE rework_jobs
SET status = 'active', closed_at = NULL, closed_by = NULL
WHERE lot_no = '69S01000105';     -- ⬅ แก้เป็น Lot ของคุณ

-- (ถ้าไม่รู้ Lot — ดูงานที่ปิดล่าสุดก่อน)
-- SELECT id, lot_no, product_name, status, closed_at, closed_by
-- FROM rework_jobs WHERE status = 'closed' ORDER BY closed_at DESC LIMIT 20;


-- ───────────────────────────────────────────────────────────────
--  B) ย้อนปิด "งานผลิตถาวร" (job_summaries) — ลบใบสรุปที่ปิดไป
--     ⚠ ม้วนใน production_rolls ยังอยู่ครบ — ลบแค่ "ใบปิดงาน"
--     หลังลบ งานจะไม่ถูกนับเป็นปิดถาวรอีก (ดึงงานเก่ากลับมาได้)
-- ───────────────────────────────────────────────────────────────
-- ดูใบสรุปก่อนลบ (กันลบผิด):
-- SELECT id, machine_no, lot_no, product_name, good_kg, closed_at, closed_by
-- FROM job_summaries WHERE lot_no = '69S01000105' ORDER BY closed_at DESC;

DELETE FROM job_summaries
WHERE lot_no = '69S01000105'       -- ⬅ แก้เป็น Lot ของคุณ
-- AND machine_no = 'S01'          -- (ทางเลือก) ระบุเครื่อง ถ้า Lot ซ้ำหลายเครื่อง
;
